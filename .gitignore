node_modules/
dist/
data/
.env
*.log
content/audio/*.mp3
content/audio/*.ogg
.playwright-mcp/

# Сгенерированная озвучка: 82 МБ, пересобирается `npx tsx scripts/generate-audio.ts`
# (нужен свой ключ Azure Speech). Образцы голосов в content/audio/samples/ оставлены
# в репозитории намеренно — по ним выбирают голос.

# Внутренние исследования и заметки — не для публичного репозитория
RESEARCH-*.md
